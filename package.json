{
  "name": "phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../fullstack-open/part2/phonebook && npm run build && cp -r dist ../../../phonebook-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lân Nguyễn Danh",
  "license": "ISC",
  "description": "part 3 of the fullstackopen course",
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.0"
  }
}
